apiVersion: v1
kind: ConfigMap
metadata:
  name: jira-agent-config
data:
  DJANGO_DEBUG: "False"
  DJANGO_ALLOWED_HOSTS: "localhost,127.0.0.1,jira-agent.local,backend"
  JIRA_INSTANCE_URL: "https://llmagent.atlassian.net"
  PROJECT_KEY: "SCRUM"
  LLM_PROVIDER: "ollama"
  # For a real cluster, replace this with an in-cluster Ollama service URL or reachable model gateway.
  LLM_BASE_URL: "http://ollama:11434"
  LLM_MODEL: "qwen3:8b"